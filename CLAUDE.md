# train-loader

A web app that advises which carriage of a Brighton–London train to board to maximise the
chance of getting a seat. See `README.md` for where the project stands and
`docs/01-feasibility.md` for what the data research established.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `richbroad29/train-loader`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root, created lazily when there is
something to record. See `docs/agents/domain.md`.
